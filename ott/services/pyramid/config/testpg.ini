###
# app configuration
# http://docs.pylonsproject.org/projects/pyramid/en/latest/narr/environment.html
###

[app:main]
use = egg:ott.services

pyramid.reload_templates = false
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.default_locale_name = en
pyramid.includes =
    pyramid_tm

sqlalchemy.url = postgresql+psycopg2://geoserve@localhost:5432/trimet
sqlalchemy.schema = ott
sqlalchemy.is_geospatial = true

atis_url   = http://localhost/maps/ageo/V1/geocode/format/json
solr_url   = http://localhost/solr
otp_url    = http://localhost/prod
advert_url = http://localhost/trimet.org/map/adverts
fare_url   = http://localhost/trimet.org/map/fares/fares.json
avert_timeout_mins = 30

# TEST INSTANCE
#sqlalchemy.url = postgresql+psycopg2://postgres@localhost:54321/trimet
#sqlalchemy.schema = ott
#solr_url   = http://maps10.trimet.org/solr
#otp_url    = http://maps10.trimet.org/prod
#advert_url = http://dev.trimet.org/map/adverts
#avert_timeout_mins = 0

[server:main]
use = egg:waitress#main
host = 0.0.0.0
port = 44444
threads = 50


###
# logging configuration
# http://docs.pylonsproject.org/projects/pyramid/en/latest/narr/logging.html
###

[loggers]
keys = root, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = DEBUG
handlers = console

[logger_sqlalchemy]
level = DEBUG
handlers =
qualname = sqlalchemy.engine
# "level = INFO" logs SQL queries.
# "level = DEBUG" logs SQL queries and results.
# "level = WARN" logs neither.  (Recommended for production systems.)

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s][%(threadName)s] %(message)s
